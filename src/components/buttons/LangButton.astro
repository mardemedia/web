---
interface Props {
  href: string;
  label: string;
  className?: string;
}

const { href, label, className }: Props = Astro.props;
---

<a href={href} class={`uppercase font-bold  ${className && className}`}>
  {label}
</a>
