---
const { className, id = "section-container" } = Astro.props;
---

<section
  id={id}
  class={`mx-auto flex w-full max-w-7xl flex-col gap-6 px-5 py-18 text-center sm:px-8 sm:py-24 lg:px-16 lg:py-32 ${className}`}
>
  <slot />
</section>
